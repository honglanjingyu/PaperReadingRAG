优化:
增量更新 无需重建整个索引
长上下文
维护同义词表  词向量聚类：将语料中的高频词/短语向量化后聚类，同一簇内的词视为潜在同义词（如“笔记本-电脑”）。
前端显示阶段: 正在检索文档->正在生成回复
幻觉  面经
多路召回:向量检索+关键字检索并行
通常两路都跑，向量检索捕获语义相关的内容，BM25 精准命中关键词，再用 RRF 算法把两路结果合并，这样覆盖面比单路宽很多。

增加RL

去重
意图识别,路由
并行,多用户
同义词表的维护 手动和自动维护
不必要记忆,上下文压缩
双向通信（如用户可中断生成），会考虑WebSocket
文档更新